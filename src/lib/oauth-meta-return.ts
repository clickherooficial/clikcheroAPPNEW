/** Onde voltar quando o OAuth da Meta não é em popup (ex.: mesmo Safari / nova aba). */
export const META_OAUTH_RETURN_STORAGE_KEY = 'clickhero:meta-oauth-return';

export const WIZARD_STEP_STORAGE_KEY = 'clickhero:wizard-step';
